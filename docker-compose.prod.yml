secrets:
  jwt_secret:
    external: true
  postgres_password:
    external: true
  google_client_secret:
    external: true
  lichess_client_secret:
    external: true

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lipair
      POSTGRES_USER: lipair
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    secrets:
      - postgres_password
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      placement:
        constraints:
          - node.role == manager
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lipair"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: ghcr.io/armatorix/lipair/backend:latest
    environment:
      DATABASE_URL: postgres://lipair:lipair@postgres:5432/lipair?sslmode=disable
      SERVER_PORT: "8080"
      FRONTEND_URL: ${FRONTEND_URL:-https://lipair.example.com}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://lipair.example.com}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_REDIRECT_URL: ${GOOGLE_REDIRECT_URL:-}
      LICHESS_CLIENT_ID: ${LICHESS_CLIENT_ID:-}
      LICHESS_REDIRECT_URL: ${LICHESS_REDIRECT_URL:-}
    secrets:
      - jwt_secret
      - google_client_secret
      - lichess_client_secret
    deploy:
      replicas: 2
      update_config:
        parallelism: 1
        delay: 10s
        order: rolling-update
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    depends_on:
      - postgres

  frontend:
    image: ghcr.io/armatorix/lipair/frontend:latest
    ports:
      - "80:80"
      - "443:443"
    deploy:
      replicas: 2
      update_config:
        parallelism: 1
        delay: 10s
        order: rolling-update
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    depends_on:
      - backend

volumes:
  postgres_data:
